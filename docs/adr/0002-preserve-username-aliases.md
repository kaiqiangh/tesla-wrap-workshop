# Preserve former Usernames as permanent aliases

Public Profile URLs and community attribution must remain trustworthy after a Username change. WrapForge therefore redirects every former Username to the same Profile and never assigns that alias to another User. This consumes namespace permanently, but avoids broken indexed links, attribution drift, and impersonation after renames.
